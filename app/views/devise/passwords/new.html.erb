 <div class="password-form">
   <h1>パスワードをお忘れの場合は・・</h1>
   <p>下記のフォームから再設定のほどよろしくお願いします。</p>
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html:{ method: :post }, html:{id:"passwords"} ) do |f| %>
      <%= devise_error_messages! %>

    <div class="field password">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, class:"bg-gray",id:"password-email",autocomplete: "email" %>
    </div>

    <div class="actions password">
      <%= f.submit "Send me reset password instructions" %>
    </div>
   <% end %>
 </div>
