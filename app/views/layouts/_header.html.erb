<% if user_signed_in? %>
  <header class="bg-white fixed-top">
    <nav class="navbar navbar-expand-md navbar-light header-container">
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <%= link_to root_path do%>
        <%= image_tag('PROPS_logo.png',class:"props-logo")%>
      <% end %>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <%= render 'searches/search_form' %>
          </li>
        </ul>
          <% if current_user.image? %>
            <%= link_to image_tag(current_user.image, class: "user-icon"), user_index_path %>
          <% else %>
            <%= link_to image_tag('default_img.png', class: "def-user-icon"), user_index_path %>
          <% end %>
          <%= link_to "投稿", new_post_path, class:"orange-button text-white bg-orange",id:"post-button" %>
      </div>
    </nav>
  </header>
<% else %>
  <header class="bg-white fixed-top">
    <div class="header">
      <div class="header-content">
        <%= link_to root_path do%>
          <%= image_tag('PROPS_logo.png',class:"props-logo")%>
        <% end %>
        </a>
        <%= render 'searches/search_form' %>
        <p class="header-left">
          <button type="button" class="like-a-tag" id="register-btn">新規登録</button>
          |
          <button type="button" class="like-a-tag" id="login-btn">ログイン</button>
        </p>
      </div>
    </div>
  </header>
<% end %>
